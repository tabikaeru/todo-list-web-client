export const Home = {
  render: async () => {
    const view = /*html*/ `
            <section class="section">
                <h1> Home </h1>
            </section>
        `
    return view
  },
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  afterRender: async () => {},
}
